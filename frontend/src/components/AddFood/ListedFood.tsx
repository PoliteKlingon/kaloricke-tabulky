import { Typography } from '@mui/material'
import React from 'react'

function ListedFood({name}) {
  console.log("jííííííídlo");
  console.log(name);
  return (
    <Typography>{name}</Typography>
  )
}

export default ListedFood