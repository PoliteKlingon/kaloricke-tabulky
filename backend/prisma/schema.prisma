generator client {
  provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url = "file:./database.db"
}

// TODO
model UserCredentials {
  id              String         @id @default(uuid())
  username        String
  passwordHash    String
}